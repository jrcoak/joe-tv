# Internal S1 checkpoint

Fresh read-only coordinator `/root/s1_checkpoint` reviewed durable reports; PM retained decisions. No new user-owned task or recurring meeting was created. Candidate f394d4d passed fresh smoke/build but failed guide-origin controls after active preview; S1 remains under correction. Home origin, Quick Switch origin, missing-favorite fallback, empty Home and sampled Sports/feed returns pass so far. QA's final checkpoint captures are pending integration.

PM selected S2 from evidence: Services owns validated cache lifecycle and measured per-document timestamp reuse; App owns a Channels-only polish based on an actual clipping screenshot. SecOps reviewed S1 fixture isolation (no source blocker, normal dependency construction reads still possible) and defined cache invariants. Design provided concrete local layout acceptance. The preview-only correction is integrated in 9a2b5df; it remains unproven until QA retests.

Candidate code is frozen per verification pass. Fixes get new exact commits and fresh affected checks. QA owns the dedicated simulator. Pure host benchmarks are separate and do not prove tvOS latency. Sidebar, favorite-team Home, provider additions/deployment and release remain later. R2 public-stream shortlist is complete without playback or catalog changes.
