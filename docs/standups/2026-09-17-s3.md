# Internal standup — September 17, S3 checkpoint

Fresh read-only coordinator: gpt-5.6-sol / high, supplied PM integration `49792c4824c5de3db84bc247c6ae2f0b8a54f6b6` while Services implemented S3. No specialist contexts replaced; no runtime or recurring schedule.

## Outcomes and decisions

- WBZ only remains a future stream candidate. Other research candidates are dropped by Joe's preference.
- S1/S2 offline checks remain passed at their exact source; visual acceptance is pending. A fresh inventory succeeded, but Simulator app selection and one supported retry timed out. Current host lock state is unknown. QA released the bounded runtime without boot/install/input.
- Services works from `a8301bc`; later PM changes are docs only. PM must verify returned scope and exact commit before integration.
- Coordinator flagged lost source ownership in the single merged schema. PM and Services agreed explicit requested-channel groups and successful-station precedence, including authoritative empty and prior station claims. Current production provider station namespaces are disjoint. Retain the existing aggregate schema; a new private persistence architecture is unnecessary for this bounded correction.
- Review must check removed channels, obsolete mappings, missing-provider handling, conservative source age and no stale-request mutation. Any lifecycle claim requires source evidence beyond pure merge tests. AppModel's existing request-ID guard/sign-out invalidation remain required.
- QA must explicitly run `scripts/test-guide-merge.sh` in addition to smoke/build; it is not implicitly included in team-check.sh. Services' own pass is not independent acceptance.

## Next owners

Services returns scoped source/tests/report. App reviews refresh orchestration and lifecycle; SecOps reviews retained-input isolation/failed-source handling. PM integrates sequentially after material issues are resolved, then QA checks the exact candidate offline. Preserved S1/S2 UI artifacts remain for their controlled comparison when access returns; neither this source review nor offline QA clears the guide input blocker or accepts Channels visually.
