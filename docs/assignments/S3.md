# S3 — preserve useful guide listings through a partial refresh

Status: assigned, bounded continuation under TEAM-019/024. Spec TEAM-4. PM supplies the exact commit containing this assignment. S1/S2 visual acceptance remains independently pending; this assignment does not accept those changes.

## Viewer result and evidence

When one guide provider fails but the other succeeds, retain the failed provider's applicable last-known listings and station mapping. Successful sources replace their own prior contribution, including a valid empty result. A mixed refresh must not label all retained data as just fetched. AppModel.refreshEPG currently reconstructs from loadedResults alone and sets fetchedAt to Date(); M0 Services S2 identified the partial-loss consequence. S2 fixed provider-level disk lifecycle, but not this orchestration. This is a correctness contract supporting responsive/resilient browsing in roadmap package 2; competitor documentation does not reveal internal cache merge policy, so no parity or numerical performance claim applies.

## S3-SERVICES ownership

Reuse Services task/worktree. New clean branch codex/joe-tv-services-s3 from PM's exact supplied baseline; preserve prior branches. Model gpt-6-astra / high for source boundary and failure semantics. Allowed edits:

- SeasonsTV/App/AppModel.swift: refreshEPG orchestration and strictly necessary private guide-only helpers/state. Preserve epgRequestID last-request guard, loading state, logout invalidation, existing request order/timeouts and navigation fixture no-op. Do not change playback, preferences, Sports or unrelated lifecycle.
- SeasonsTV/Models/Models.swift: guide-only pure merge policy/types adjacent to EPGGuideWindow if needed. Existing domain models and interfaces remain source-compatible. Do not change public provider behavior or add per-source persistence architecture.
- Tests/GuideMergeSmoke.swift and scripts/test-guide-merge.sh: isolated deterministic host test entrypoint; wire the runner using existing smoke conventions and explicit dependencies without editing team-check.sh or project settings.
- docs/sprints/S3-services.md: exact source, contract, results and limitations.

No networking/provider code, UI/player, shared ParserSmoke, project file, backend/production/remote access, credentials, team docs or simulator operations. QA has exclusive simulator access. Services may run its own host test compilation; do not run an app build or benchmark during QA's runtime session.

## Acceptance and scope limits

- Reproduce the original loss with a two-source known-good snapshot followed by one success/one failure. The final production orchestration must use the tested merge policy, not duplicate it separately in tests.
- Track source ownership through the explicit requested channel groups and mappings. Keep a failed source's still-relevant cached programs/mappings; replace a successful source's data authoritatively. Do not resurrect successful-empty data, removed channels or unrelated stations; retain sorted program order and stable identities. Honor requested time overlap; do not invent guide coverage or program times. A known long-running program may bridge disjoint old/new viewports if its unchanged interval overlaps both. Outside viewport overlap, retain a failed mapping only if such an applicable program proves relevance; empty/mapping-only data is dropped. Explain treatment of shared stations and obsolete mappings.
- Carry successful result fetchedAt values into the merged result. Retained content uses its existing provenance timestamp; a conservative oldest contributing timestamp is acceptable under the existing single Date schema. Never substitute Date() to disguise cached/unknown age. Record the single aggregate timestamp limitation honestly.
- Failure remains a failed state with the existing safe error surface, while applicable cached content remains selectable. All-success, valid empty, all-failure/no-cache, no-provider/no-channel, out-of-order completion and sign-out invalidation preserve intended behavior. Do not introduce retry loops or clear valid content just to update status.
- VeryLocalClient can suppress per-station failures internally; this assignment addresses failures visible to AppModel only. Do not expand into station-by-station provider retry architecture. Provider absence handling must be explicit and tested/reviewed.
- Meaningful tests: either source fails, both succeed with changed mapping/data, authoritative empty, no usable cache, window/removed-channel filtering, stable mapping/program ordering, retained/unknown fetchedAt, recovery, and aggregate partial result. Include direct source audit of stale request/signout guard if the host harness cannot instantiate AppModel safely. Do not claim a pure policy test proves async AppModel lifecycle or on-screen behavior.

Return a small clean commit, test evidence, any unresolved semantics, and no runtime claims. PM integrates after read-only App/SecOps review and QA independently runs relevant host/build checks at the integrated source. The retained S1/S2 artifacts remain the UI acceptance comparison until explicitly replaced.

## Independent review and QA

App receives an exact Services commit for read-only review of refreshEPG wiring, stable mapping/row behavior, request-ID and sign-out guards, and current UI consumers. Own only `docs/sprints/S3-app-review.md` on a clean new review branch from the supplied commit. SecOps separately reviews the same immutable source, requested channel/station filtering, failure/error/age semantics and isolated test boundaries; own only `docs/sprints/S3-secops.md`. Neither reviewer edits Services source or runs simulator/network probes. Report material corrections to PM before any follow-up implementation; no ceremonial approval beyond applicable source review.

QA receives PM's exact integrated candidate after review. Own `docs/sprints/S3-qa.md` on a clean new branch. Run `scripts/test-guide-merge.sh`, `scripts/team-check.sh smoke`, then `scripts/team-check.sh build` freshly and sequentially. S2 cache tests need not be repeated unless the S3 review identifies relevant provider/runner behavior changes. Save a separately identified S3 app artifact; preserve S1/S2 artifacts and hashes. No simulator/UI retries in this offline assignment; exact S3 runtime acceptance remains pending supported access. Pure policy/source checks do not prove async lifecycle, real service failure, or visual behavior.
