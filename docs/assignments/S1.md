# S1 — ordinary watching and returning

Status: implementation authorized and in progress.
Spec: TEAM-4. User direction: “just keep plugging away on things.” PM selects the first small UI/navigation/performance package from the completed roadmap.
Prior integrated baseline: d0c914a733c69b64f46ee366b38c5589562a0bd7. Release source remains 138c271/build 7.
Starting commit: the commit that adds this assignment; PM supplies its exact SHA in each task prompt.

## Outcomes

- Back from the focused Fantasy scorebug goes through the existing one-layer handler; preserve repeated-input protection.
- Return from playback restores the originating Home/guide/Sports context when valid, including selected guide program/row. If gone, choose a nearby relevant fallback. Preserve filters and browsing position.
- Current guide-program Select tunes directly; future/expired programs retain truthful details. Preserve explicit baseball feed selection and current channel actions.
- Empty Home has a direct Choose Favorites action into existing settings and returns predictably.
- Establish measurable browsing evidence with controlled data; change performance-sensitive code only when evidence identifies a cost. Do not claim production speed from empty-player fixtures.

## Ownership and models

| Assignment | Owner | Allowed edits | Model |
| --- | --- | --- | --- |
| S1-APP | tvOS App | Views/RootView.swift, Views/JoeTVExperience.swift; AppModel.swift only focus/bookmark reconciliation and Debug navigation-fixture setup; docs/sprints/S1-app.md | gpt-6-astra / high |
| S1-PLAYBACK | Playback | Views/PlayerScreen.swift Back/focus handling only; docs/sprints/S1-playback.md | gpt-5.6-sol / high |
| S1-DESIGN | Product Design | docs/sprints/S1-design.md; source read-only | gpt-6-astra / high |
| S1-QA | QA | docs/sprints/S1-qa.md; independent checks; no app code edits | gpt-6-astra / high |
| S1-PM | PM | assignment/spec/roster/summary; integration; bounded profiling helpers under scripts if justified | current PM model |

App is the sole AppModel editor in this sprint; report exact touched symbols. No worker edits Models.swift, ParserSmoke.swift, DesignSystem.swift, project/signing/build settings, networking or DRM without a revised explicit allocation. Playback must not edit RootView/JoeTVExperience/AppModel. Design reviews interaction intent and touched surfaces; report issues to PM, no application edits. Services/SecOps remain available if the scope reaches their boundaries; this sprint assigns no network/auth/security-sensitive changes.

Use new codex/joe-tv-<role>-s1 branches in the existing role worktrees from PM's exact supplied baseline, only after clean status/branch absence checks. Preserve completed branches and all pre-existing changes. No resets or cherry-picking setup history into mismatched branches. Share Git-verified full commit IDs.

## Design and research inputs

Read the relevant roadmap package and docs/research/evaluation-method.md plus streaming-discovery.md conclusions1/2/6. This is the accepted small first sprint, not a new research exercise. Useful competitor patterns have been compared already; none establishes a universal sidebar rule. Preserve adopted RALLY direction, focus/selection distinction, existing hint for Hold Select/Last Stream, full-width Fantasy video and custom player.

Current-program activation is assessed against actual time at selection, not stale cached “live” status. Future-program details must label any Watch Channel action as watching the channel now. Direct favorites settings must use the existing route and preserve settings behavior. Minimal local polish may support these interactions; avoid a broad reskin.

## Runtime and verification

QA is the only simulator/profiler owner for S1, on existing dedicated Joe-TV Team QA simulator C95B257D-0111-40A1-9D02-2AD70D850FF8. Session begins on assignment; checkpoint at baseline handoff and final integrated handoff; stop app and shut it down at completion. Do not operate or erase existing user simulators.

Baseline can reuse the previously built fixture artifact only if its exact app source equivalence is verified and its build number/limitations recorded. Final acceptance must build/test the exact integrated S1 commit. No real authenticated streams, credentials, private-config copying, live provider/API requests or FairPlay probes. Public artwork loading in existing fixtures must be labeled if present. A populated static guide fixture may be added by App for meaningful current/future/expired and focus coverage.

QA: record existing scorebug issue and guide-return case as baseline evidence; exercise Home/watch/Back, at least two guide origins, current/future program selection, empty Home/settings/return, Sports/feed/Back and Fantasy controls/drawer layers. Test repeated and spaced Back, no tuning on focus, removed-item fallback and preferences. Treat physical Siri Remote, FairPlay and real-media startup as pending hardware evidence.

Use team-check smoke and unsigned Debug simulator build on the integrated candidate. For profiling, name commit, configuration, simulator/hardware, data size, cache and method. Instruments traces or exact command timing may be used when meaningful; screenshot/tool latency is not app input latency. If full timing cannot be measured reliably, report that limit and deliver only supported findings; do not perform speculative performance rewrites.

## Exit and continuation

PM integrates bounded commits sequentially, inspects diffs and asks independent QA to exercise the assembled candidate. Fix regressions before reporting completion. Save a short demo/evidence report and exact source/build baseline. Local implementation and commits are authorized; pushing, TestFlight upload and production rollout are separate delivery steps.

User now authorizes continued useful work, so PM may select subsequent small roadmap slices within UI/performance/reliability without repeatedly asking to begin. Keep each slice reviewable and tested; do not treat this as authorization for a major redesign, unattended recurring schedule or unbounded feature expansion. Sidebar, team Home and other large features remain later directions.
