# S5 — repair Sports filter layout

Joe reported the Filter Sports sheet broken in a September 17, 11:55 AM screenshot while using the configured S4 app. The image shows oversized semantic text, unequal row heights, three-line College Football / NCAAF, and footer crowding. This is an observed layout repair, not a new product direction; competitor parity does not apply.

Baseline: `7fe97fcddd32630f27de2e74ea2f70e53bb017d5`, with S4 source and SecOps controls. Preserve all26 categories, saved selections, counts, Select All and dismissal semantics. No changes to sports phase, provider/player/configuration or other sheets.

## Ownership

- App: `codex/joe-tv-app-s5`, same worktree; gpt-6-astra/high. Own only JoeTVSportsFilterView and strictly scoped style/helper in JoeTVExperience.swift, plus docs/sprints/S5-app.md. Explicit TV-appropriate typography, uniform padded tiles, room for long category names, bounded scrolling with header/footer visible, focus treatment not clipped, singular/plural count. Preserve accessibility and actions. Run syntax/diff checks; no app build/runtime.
- Design: gpt-6-astra/medium. Review screenshot/source read-only, send concrete guidance; own docs/sprints/S5-design.md if a report is needed, on codex/joe-tv-design-s5. Review final simulator screenshots; no runtime or code changes.
- PM: integrate sequentially and review scope. Routine visual repair does not need a new SecOps gate; keep the accepted configured build method.
- QA: finish S4 evidence first; then build the exact supplied S5 candidate via scripts/build-configured-simulator.py with original Debug Private.xcconfig, preserve earlier artifacts and redacted logs. No broad test rerun is required for an isolated layout edit unless a relevant change/failure warrants it. Visually verify long labels, first/middle/last rows, focus while scrolling, toggle/reopen persistence, Done and Back, and footer reachability. Source checks may cover destructive-to-preference Select All unless original selections can be exactly restored.

## Runtime and delivery

The simulator is currently open for Joe. QA stopped when CUA detected his interaction. PM will announce a brief new exclusive session before QA replaces the app and checks this repair. Only dedicated device C95B257D-0111-40A1-9D02-2AD70D850FF8; preserve login/preferences; no reset/delete/global simulator operations. Normal app mode, no credentials, playback or provider probes. Retain S4 live-status/configuration correction and leave the fixed filter sheet open for Joe after evidence. User input takes precedence; stop if concurrent interaction is detected.
