# S4 — truthful live-game status and a configured browsing build

Status: implementation authorized by Joe: “ok. fix it though….” Baseline is the PM commit containing this assignment; app source is unchanged from `9d3cd8a`. Read `docs/assessments/live-status-diagnosis.md`. Deliver a fixed, configured local simulator app, with tests and normal-session evidence. Do not stop at diagnosis. No new channels, release/upload, backend change, provider playback test or credential rotation.

## Product contract

A playable feed is not proof of a live game. Add `SportsEventPhase.unknown` for insufficient timing/phase evidence. Unknown entries must not inflate Live or Upcoming counts; do not relabel them as scheduled, replay or final. The current two-tab game guide can omit unknown rows and use a brief neutral notice when appropriate; this correction does not add a third destination or redesign Sports. Existing direct channel access remains independent.

Preserve structured start/end times and determine phase at the supplied clock. Generic Live/Upcoming labels, especially parser-generated labels, cannot override future starts or known ended windows, or freeze across time boundaries. Explicit final/replay signals remain excluded. Keep the existing bounded no-end window unless a concrete source contract warrants a change; no new status-freshness SLA or provider polling. Unknown/delayed/canceled and conflicting status behavior must be explicit, without substring mistakes such as quarterfinal being final. Genuine game status still matters within supported timing evidence; do not claim a daily snapshot proves real-time scores. Preserve the separate 15-minute pregame playback rule: an upcoming game can be watchable before it is live.

## S4-SERVICES

Existing Services worktree, new clean `codex/joe-tv-services-s4` from supplied baseline; gpt-6-astra / high. Own:

- `SeasonsTV/Models/Models.swift`: SportsEventPhase, MediaItem sports phase/availability helpers, related SportsEventGuidePolicy filtering only.
- `SeasonsTV/Networking/HTMLCatalogParser.swift`: sports timing/phase derivation and narrowly relevant studio exclusions only. Stop ESPN+ parse-time generated Live/Upcoming; retain replay evidence and structured times. Support valid existing timestamp forms including fractional ISO where applicable. Preserve provider identities/date matching, stream resolution and feed choices. Do not invent absolute dates from localized display-only strings.
- `Tests/ParserSmoke.swift`: affected sports phase/parser tests and fixed-clock regressions; preserve unrelated tests.
- `docs/sprints/S4-services.md` concise handoff.

No AppModel/UI/player/config/shared docs/runtime/network/private configuration. App owns exhaustive UI switches and AppModel playback mapping in parallel. Add unknown first so that shared contract is stable. Report exact source commit promptly once targeted/full smoke passes; no app build or benchmark. Fixed-time cases must cover missing enrichment/time, future+Live, ended+Live, stale in-progress outside bounds, no-end beyond existing window, parser-time boundary advancement, invalid/fractional dates, final/replay, relevant genuine in-game statuses, studio exclusions, and pregame start−901/start−900/start. Tests asserting timeless ESPN+ is Live must change. No need for new implementation-mirroring tests.

## S4-APP

Existing App worktree, new clean `codex/joe-tv-app-s4`, gpt-6-astra / high. Own:

- `SeasonsTV/Views/JoeTVExperience.swift` and `SeasonsTV/Views/RootView.swift`: exhaustive phase switches, neutral unknown copy and a concise unknown-listing notice if needed. Unknown is “Time unavailable” / “Schedule unavailable”, never Live/Up next/Final. Confirmed Live and Upcoming grids/counts use Services policy. Preserve navigation, focus, feed selector, filtering, existing IDs and S1/S2 changes.
- `SeasonsTV/App/AppModel.swift`: only `makePlaybackSession` sports-phase switch plus strictly necessary unknown-case compatibility. Unknown event timing must not mark it live for history/guide predicates; when an available live transport is selected elsewhere, retain existing live-edge behavior independently of event labeling. No playback/network lifecycle refactor.
- `docs/sprints/S4-app.md` concise handoff.

Models/parser/tests are Services-owned. No PlayerScreen edits unless a concrete requirement is brought to PM for Playback assignment; existing enum usage may not require any. No runtime, app build or private config. Run syntax/diff checks; source from parallel branch may not yet have enum case, so final typecheck belongs to integrated QA.

## PM / QA configuration and delivery

PM owns configured build plumbing. Use only the existing exact Debug config `/Users/joecoakley/SeasonsTV/Config/Private.xcconfig`; no wildcard copy, new token, Internal/Release credential, or printing config contents. Prefer passing an explicit existing config path over copying secrets. Capture build output through redaction before saving/returning it, because Xcode can print resolved settings. Preflight the resulting bundle for configuration shape without printing values; successful metadata load is a separate runtime observation.

QA independently runs full smoke and guide merge regression, then builds the exact integrated candidate in its own cache using the approved configuration/log redaction method. Preserve earlier artifacts. No source edits. Joe's explicit fix request authorizes replacing/relaunching the app on the dedicated QA simulator `C95B257D-0111-40A1-9D02-2AD70D850FF8` when ready. Announce replacement before doing it, preserve login/preferences, and leave normal-mode app open for Joe afterward. Do not erase/reset or touch other devices. One owner at a time; PM supplies the runtime grant after offline checks. Inspect Sports normally: missing-configuration banner gone, loaded schedule evidence, no unknown/timeless/future game in Live, studio examples excluded, future coverage still Upcoming. Use no real playback or direct provider probes. If metadata authorization/server errors appear, report exact sanitized failure and diagnose without retry bursts or token changes. App/UI acceptance of this repair is separate from all remaining S1/S2/device checks.
