# Joe-TV 1.0 (8) — September 17, 2026

Status: main pushed and App Store Connect upload succeeded at approximately 12:24:35 EDT. Apple's final upload response was “Uploaded package is processing”, “Upload succeeded”, and “EXPORT SUCCEEDED”. TestFlight processing completion/availability was not independently verified; the browser session was signed out. No tester groups or external distribution settings were changed.

Joe explicitly authorized merging the verified team changes to main, pushing, and uploading a new TestFlight build. Release source: `bf115abff8522814322f75c60deefcfb285411b9` on main. GitHub origin/main was current at `138c271`; integration was a fast-forward through the complete team history, followed by the build7→8 commit. Source in SeasonsTV, Tests, scripts and Config is identical to tested application candidate `98844e3ebdf82396011be1365da630a9addd1ddd`. Only project build number and release decision documentation changed before the archive.

## Included changes and evidence

Sports uses structured timing instead of feed playability to determine Live, preserves upcoming/pregame behavior, and excludes the observed studio titles. Filter Sports now has explicit typography, equal tiles, complete long names and separated header/footer. Earlier team cycles improve watching/return focus paths, Channels readability, validated caches, guide parsing and partial-refresh continuity. Details and precise coverage are in `../sprints/quality-cycles.md` and S1–S5 reports.

S4/S5 independent parser smoke, configuration-helper tests, guide-merge checks and configured simulator builds passed at their recorded candidates. Latest final smoke/build passed at98844e3; normal Sports/filter runtime checks and Design review passed with documented limits. Release archive and bundle preflight freshly passed for1.0/build8, physical tvOS, bundle `com.jrcoak.joetv`, expected Internal metadata token and HTTPS API configuration. Source and the one-release archive/upload wrapper received bounded SecOps acceptance. Release compiler emitted unreachable DEBUG-fixture-branch warnings; archive succeeded. No new source changes were made to address benign warnings during release.

## Delivery

Used the existing Release→Internal.xcconfig configuration and existing Apple signing team. No token was copied into tracked files, printed or passed on the command line. Build output was redacted before retained log writes. Export reused the previous build's app-store-connect/upload/automatic-signing options, with build-number management disabled, and exited0. No production service or credential changes.

Local archive: `.build/releases/1.0-8/Joe-TV.xcarchive`. Redacted archive/upload logs and value-free receipts are in the same ignored directory. The archive contains the intended runtime read token and remains local sensitive build material. GitHub main includes the code and this release record, not the archive or private configuration. Unrelated SHELF is untouched. The configured simulator app remains the previously verified build7 artifact for Joe; the new physical-device/TestFlight archive is build8.

## Device follow-up

Verify FairPlay streams, physical Siri Remote/rapid Back behavior, captions and couch readability on Apple TV. The remaining earlier guide-controls/Channels and controlled async-lifecycle acceptance limits remain explicit; simulator/build success does not certify those journeys. No hardware playback was run for this release.
